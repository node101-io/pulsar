# Installs must match pnpm-lock.yaml exactly — no silent re-resolution.
# To change dependencies: edit package.json, then run
#   pnpm install --no-frozen-lockfile   (or `pnpm add ...`, which updates the lock)
# and commit the lockfile diff.
frozen-lockfile=true
