import { PublicKey } from 'o1js/dist/node/lib/provable/crypto/signature.js';
import { NetworkId } from 'o1js/dist/node/mina-signer/src/types.js';
import { currentTransaction } from 'o1js/dist/node/lib/mina/v1/transaction-context.js';
import { type FeePayerSpec, type ActionStates, type NetworkConstants, activeInstance, setActiveInstance, Mina, currentSlot, getAccount, hasAccount, getBalance, getNetworkId, getNetworkConstants, getNetworkState, fetchEvents, fetchActions, getActions, getProofsEnabled } from 'o1js/dist/node/lib/mina/v1/mina-instance.js';
import { Transaction, type PendingTransaction, type IncludedTransaction, type RejectedTransaction, type PendingTransactionStatus, type PendingTransactionPromise, transaction } from 'o1js/dist/node/lib/mina/v1/transaction.js';
import { filterGroups } from 'o1js/dist/node/lib/mina/v1/transaction-validation.js';
import { LocalBlockchain, TestPublicKey } from 'o1js/dist/node/lib/mina/v1/local-blockchain.js';
export { LocalBlockchain, Network, currentTransaction, Transaction, type PendingTransaction, type IncludedTransaction, type RejectedTransaction, type PendingTransactionStatus, type PendingTransactionPromise, TestPublicKey, activeInstance, setActiveInstance, transaction, sender, currentSlot, getAccount, hasAccount, getBalance, getNetworkId, getNetworkConstants, getNetworkState, fetchEvents, fetchActions, getActions, FeePayerSpec, ActionStates, faucet, waitForFunding, getProofsEnabled, filterGroups, type NetworkConstants, };
/**
 * Represents the Mina blockchain running on a real network
 */
declare function Network(graphqlEndpoint: string): Mina;
declare function Network(options: {
    networkId?: NetworkId;
    mina: string | string[];
    archive?: string | string[];
    lightnetAccountManager?: string;
    bypassTransactionLimits?: boolean;
    minaDefaultHeaders?: HeadersInit;
    archiveDefaultHeaders?: HeadersInit;
}): Mina;
/**
 * Returns the public key of the current transaction's sender account.
 *
 * Throws an error if not inside a transaction, or the sender wasn't passed in.
 */
declare function sender(): PublicKey;
declare function waitForFunding(address: string, headers?: HeadersInit): Promise<void>;
/**
 * Requests the [testnet faucet](https://faucet.minaprotocol.com/api/v1/faucet) to fund a public key.
 */
declare function faucet(pub: PublicKey, network?: string, headers?: HeadersInit): Promise<void>;
