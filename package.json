{
  "name": "pulsar-monorepo",
  "private": true,
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "build": "pnpm --filter pulsar-contracts build && pnpm --filter pulsar-chain-client build && pnpm --filter pulsar-prover --filter pulsar-bridge --workspace-concurrency=1 build",
    "test": "pnpm -r test"
  }
}
